# 🕷️ MASTER STATE — Execution Only

**v2.29 · Updated July 28, 2026 (AWIP/IMP demo reseed STAGED — wipe SQL + 3 scripts built and handed off; Meridian content replaced the moment Brian runs the sequence) · REPLACES all prior versions in full**
Strategy/positioning: STRATEGY.md (v3) · Core IP: ELICITATION-ENGINE-SPEC.md + ADDENDUM-2026-07-22 (v2) · Doc map: DOC-INDEX.md · North star: ENTERPRISE-OPERATING-BRAIN-CONCEPT-2026-07-21.md · Plan of record: ROADMAP-operating-brain-2026-07-22.md (v4)

⚠️ **System clarification (unchanged, still true):** This app (Supabase/Next.js) and the Airtable base `appV7vsagYFlfxLsG` ("LIT Spiderweb") are two SEPARATE systems. Airtable is a manually-curated LIT content + sales-demo tool — NOT this app's database.

---

## ⚡ 30-SECOND VERSION

⭐⭐ **AWIP/IMP DEMO RESEED — STAGED (July 28), PENDING BRIAN'S RUN.** The Meridian demo org becomes **All Weather Insulated Panels (AWIP) — DEMO**: 15 experts with Brian's real names (two fictional: Dana Whitfield = Quality, Tyler Brooks = coaching subject), the 10 IMP frameworks inserted **VERBATIM** from `IMP-DEMO-FRAMEWORK-CONTENT.md` (no model render — approved judgment text ships unaltered), both OPEN conflicts (**spine:** Dana Whitfield "The No-Release Gate" ⚔ Brian Ng "The Controlled Restart Release" · **cross-functional:** Kim Harrell "Custom Profile: Promise It or Walk" ⚔ Brian Montes "Capacity Reality First"), the peel-check prescription escalation (rung-2 job aid → rung-3 hands-on drill under Richard Jenkins) + one proven-effective (coil-lot re-qualify checklist), Win Column = Marcus Webb (3 leader write-ups: Kim/Montes/Chuck), one OPEN knowledge gap (between-setpoints panel thickness — Brian Ng fills it on camera), Tyler Brooks coaching watch → Chuck Milner only. **Run sequence (Brian, in order):** `claude/AWIP-RESEED-WIPE.sql` in the Supabase SQL editor (one block; DO-block safety gate aborts everything unless +test1 = 29 sources/1,248 insights and the org is still Meridian) → `node scripts/seed-awip-demo.mjs` → `node scripts/backfill-pattern-embeddings.mjs` (mandatory — the seed's embedding step is unreliable) → `node scripts/verify-p3.mjs` (gate: all 10 seeded frameworks must show embedded before continuing) → `node scripts/seed-awip-conflicts.mjs` → `node scripts/verify-awip-demo.mjs` (the DONE test — checks cascade protection FIRST, then no-Meridian-left, 15 experts, 10 embedded frameworks, the shoot query, both conflicts OPEN, prescriptions, Win Column, gap, coaching watch) → `node scripts/diag-awip-reseed-introspect.mjs` (spot-check: +test1 untouched, global insights still 1,272). New AWIP logins: `<first>.<last>@awip-demo.example` / `Demo-AWIP-2026!`; old Meridian accounts survive but are un-hooked. Details: DECISION-LOG 2026-07-28.

⭐⭐ **VIRIDESCENT REBRAND — CLOSED LOCALLY (July 27, session 3).** Humanbloom → **Viridescent** · tagline **KNOWLEDGE THAT APPRECIATES** · never "AI for human potential" in customer copy. The session-3 handoff arrived STALE — Jobs 1–3 were already complete in the tree; session 3 verified everything by grep + file reads, then finished the tail. **Palette:** zero stray hexes in `src/` outside the deliberate set — `src/styles/theme.css` (source of truth, WCAG ratios in header) + its verbatim hex mirrors (`remotion/brand/tokens.ts`, `framework-pdf.tsx`, `resume-pdf.tsx` — react-pdf/Remotion can't read CSS vars) + two explanatory comments (`ask/page.tsx:457`, `simulate/page.tsx:41`). `MarketingHome.tsx` fully token-converted; `InsightVideo.tsx` imports `BRAND`. **Naming:** every user-visible string is Viridescent — `layout.tsx` titles/OG/twitter, `.docx` creator, resume-PDF author + footer, framework-PDF footer, all 4 ad caption lines, `Mark.tsx` wordmark. Survivors are code comments only (deliberate). **Logo:** all 8 PNGs live in `public/brand/` and wired — `layout.tsx` icons/OG, `manifest.webmanifest`, `BrandHeader.tsx` (horizontal), `login` (stacked) — and **`LOGO_SRC` is flipped** to `"brand/viridescent-stacked-reversed.png"`, so the ad renders the real mark, not the wordmark fallback. Also fixed a pre-existing TS2322 in `LogoStills.tsx`. `tsc --noEmit` clean (re-confirmed at v2.28 write). Browser-verified on LOCAL dev: title "Viridescent — Knowledge That Appreciates", login logo 200, all 8 brand assets + manifest 200. **Job 4 camera pass: code-level CLEAN (no emails rendered, no Humanbloom, no placeholder text, sign-out present on `/dashboard` + `/settings`); the authenticated VISUAL walk is blocked on login — Brian logs in once, Claude drives.** ⚠️ **ALL UNCOMMITTED — live still shows the OLD branding until commit + deploy from Brian's PowerShell.** Details: DECISION-LOG 2026-07-27 (session 3 close-out).

⭐⭐ **AD-PREP DONE (July 26–27) — both pre-shoot blockers cleared.** **Job 1 (LIVE data audit):** die-changeover storyline verified camera-ready end to end — conflict row OPEN, both sides (Priya Nair "The No-Exceptions Gate" vs. Angela Brooks "The Quarantined Restart Play") read as real operator prose with real names · Tom Whitfield's frameworkless recurrence record framed + embedded via the real pipeline ("The No-Gate Check") · Tom's empty abandoned session deleted (guarded) · retrieval verified on the authed RLS path: **shoot query = "We had a quality escape right after a die changeover on the press line — should we release the next production run before first-piece inspection clears?"** → "The Quarantined Restart Play" — Angela Brooks, 0.850 top similarity, 4 results · sign-out already shipped (P-5). ⚠️ **+test1 account deliberately NOT deleted — see the CRITICAL gotcha below.** **Job 2 (LOCAL Remotion brand system):** reusable component set in `src/remotion/brand/` (OpeningTitle · LowerThird · EndCard · CaptionTrack · Mark · ScreenFrame, tokens verbatim-mirrored from theme.css) + `Ad90` composition; `npm run render:ad` → `out/humanbloom-ad-90.mp4` (1920×1080/30fps/90s) **passed locally**. All content lives in ONE file Brian edits: `src/remotion/ad/ad-script.ts`. *(Logo step superseded July 27: the Viridescent PNGs exist and `LOGO_SRC` is already flipped — see rebrand item above.)* Typecheck clean. **Uncommitted — needs a commit from Brian's PowerShell.** Details: DECISION-LOG 2026-07-26/27.

⭐ **DEMO-SCOPE HIDE PASS — DONE (July 25).** Six Track A / legacy surfaces are HIDDEN (not deleted) for the enterprise demo: resume builder · Expert Credibility Score · Knowledge Graph's Value card · profile verification · pending-insights counter · upload/approve links. Method: single flag `HIDE_TRACK_A` in `src/lib/demo-scope.ts` — render-only gating in `dashboard/page.tsx` plus a logged-in `/` → `/dashboard` redirect (Brian's call; the legacy homepage held the counter, the /upload dept card, the pricing grid, and Emerging patterns). Every hidden block carries a `// hidden for Track B demo, recoverable` marker; flip the flag to false and ALL of it returns — no components, routes, or tables touched, all still in the repo and reachable by direct URL. **#6 dependency check:** codify, all seeds, and every Track-B surface (library/retrieve/prescriptions/win-column/coaching) are pattern_records-only — zero dependence on the insights upload/approve pipeline. Dashboard now shows exactly the demo spine: Codify · Ask the brain · Team Library · Coaching Watch. Typecheck clean. Details: DECISION-LOG 2026-07-25 (demo-scope hide pass).

⭐ **VISIBLE-TEXT BRANDING PASS — DONE (July 25), SUPERSEDED by the Viridescent rebrand (July 27).** The July-25 pass unified UI text on "Humanbloom" (one word) and retired "Spiderweb" from UI copy — every "your Spiderweb" phrasing is now **"your Knowledge Graph"** (Brian's locked customer-facing label; "Trellis" stays internal-only, never in UI). "Ask the brain" phrasing kept per Brian. 🕸️/🌱/🌸 emoji kept as decoration. The company-name half is now superseded: user-visible text says **Viridescent** (July 27). **The code rename remains PARKED** ("language now, code later"): repo name, `spiderweb-nine.vercel.app`, Inngest id, prompt keys, download filenames, and all identifiers untouched. Details: DECISION-LOG 2026-07-25 (branding pass) + 2026-07-27 (session 3).

⭐⭐ **/api/retrieve — INVESTIGATED, HARDENED, NOT REPRODUCED (July 25).** Reported symptom was a contradictory `{ frameworks: [], noMatch: false }` — empty results while the route claimed matches, painting a blank page. **Full live verification found the path healthy end to end:** logged in as Tom Whitfield on the deployed app, all 5 `/retrieve` demo scenarios returned frameworks (3–5 each, top similarity 0.850 / 0.817 / 0.789 / 0.782 / 0.770), the unrelated SaaS-pricing query correctly returned `noMatch:true` at 0.687, and the UI rendered 3 cards with the ⚠️ Contested badge and "Strong match · 85%". `scripts/diag-retrieve.mjs` (new) confirmed the DB path agrees at both privilege levels for David Chen: RPC returns 3 ids above 0.75, the record load resolves 3 of 3, 0 unresolved. **Note the reported response key `frameworks` does not exist anywhere in the codebase** — the route returns `results` — so the report was reconstructed rather than copied, and the exact reported state was never observed. **The structural defect that COULD produce it was real and is now gone:** `noMatch` was computed from the search RPC's count while `results` came from a SECOND, separate record-load read — two sources of truth that could disagree. `noMatch` is now DERIVED from the array actually returned, so `{noMatch:false, results:[]}` is unconstructible; a matched-but-unloadable id returns a loud `500 RETRIEVAL_VISIBILITY_GAP` and a bad RPC shape returns `RETRIEVAL_BAD_MATCH_SHAPE`, instead of a blank page. Every stage logs a `[retrieve]` breadcrumb. **Still open: local (`npm run dev`) was never reproduced or verified — live only.** (July 26–27 note: the ad-audit re-verified the authed retrieval path healthy on live — 6/6 queries returned correct results.)

⭐⭐⭐⭐ **P-6 COACHING WATCH — SHIPPED (July 25).** Manager-only, private early signal: 2+ concern/friction pattern_records naming the same registered person surface to their direct manager, before it becomes a documented failure — never to peers, never to the named person themselves, never folded into the Win Column. Genesis: Brian asked for a feature to catch "poor performers or performers who need retraining at the moment it's identified, not after they make a mistake" — resolved as a NEW manager-only surface rather than extending the Win Column, specifically to avoid contaminating the wins-only rollup guardrail with concern/friction data. Built: `manager_id` on `profiles` + `is_manager_of()` SECURITY DEFINER RLS function · `retraining_signals` table (service-role write only, manager-scoped read/update) · `src/lib/retraining-watch.ts` detection core (exact-normalized-string person matching, same conservative matcher as the Win Column) · `assertConcernFrictionOnly()` guardrail, mirroring `assertWinsOnly()` — proves every evidence record backing a signal really is concern/friction, never win/broke/judgment · `/api/coaching/detect` route · `/coaching` page (private, amber-toned cards, never red — deliberately not punitive) · dashboard tile. **RLS is the real gate, browser-verified three ways:** David Chen (Tom Whitfield's actual manager) sees the signal ✓ · Angela Brooks (Tom's peer, same manager) sees nothing ✓ · Tom Whitfield himself (the named person) sees nothing, confirmed empty at the network/REST level, not just hidden in the UI ✓. Two design calls, both Brian's: **threshold = 2+ records** (never a single record) · **transparency = manager-only, always** (the named person is never told, under any circumstance, not even when a manager acts on it). Scope constraint discovered mid-build: only covers people who are BOTH a registered account AND have a `manager_id` on file — `entity_map` stores people as free-text names with no account link, so an informally-mentioned person (e.g. "Marcus Webb," never a login) has no account to gate access around and can never appear here.

⭐⭐⭐⭐ **THE PROTOTYPE IS DONE (P-5, July 25). BUILD-FIRST GATE LIFTS — WARM PILOT OPENS.** All 5 phases (P-1 through P-5) DONE + DEPLOYED + seed-verified + browser-verified. P-5 closed every punch-list item: sign-out button (+ found/fixed an unstyled `/login` page while testing it — now matches the app's design) · ROI ranking now sorts by urgency first (conflict > entity recurrence > coverage gap) with ROI as tiebreak, so a live contradiction never sinks to the bottom of the queue again · escalated prescription cards regenerate their pairing text and ROI rationale on escalation (no more stale rung-2 language after a rung-3 escalation) · a PASSING teach-back (98/100) now seeds alongside the failing one, so the demo shows both outcomes · `/library` count reconciled and stable: **24 org-scoped complete pattern_records, 25 globally** (includes Brian's personal account's 1 record) — confirmed identical across 4 separate back-to-back reseed runs this session · "Yours" badge browser-confirmed with no leak/mislabel from a non-author login · `/retrieve` placeholder now rotates through 5 scenarios instead of one static string · outcome-nudge (6-month "still holding?" follow-up) built, live, and confirmed correctly silent until the window elapses · demo conflict replants clean on every reseed (confirmed OPEN both runs) · Win Column decision: **mid-demo reveal, not pitch-lead** (Brian's call — see DECISION-LOG) · full demo script drafted (locked order, live-call vs. seeded annotations, one-line fallback per live step) and timed in a live dry run against the deployed app: **5:02 total**, with codify-a-win capped to exactly 2 interview turns to keep the live-call budget on track. **Next: warm pilot outreach — no build blockers remain.**

⭐⭐⭐ **THE WIN COLUMN IS COMPLETE AND LIVE (P-4.5, July 24).** Mention-based recognition: experts author frameworks, everyone else named IN those frameworks gets seen without writing anything themselves. Ranked mention view · N-expert corroboration badges ("Cited by 3 experts," the headline signal, not raw count) · extractive context chips (verbatim quotes, never model-paraphrased) · cross-dept impact badge · rising signal (accelerating mention cadence) · retention watch (named win-drivers gone quiet, framed as a check-in prompt, never a judgment) · one-click evidence packet (copyable/printable promotion case). **Wins-only rollup enforced in code, not convention** — `aggregateWinColumn()` in `src/lib/win-column.ts` is the single place pattern_records become person-level data, and it filters to `trigger_type='win'` before anything else happens; both API routes additionally scope their own queries the same way (defense in depth). Seed-verified AND browser-verified live: a planted failure record naming the org's most-corroborated person (Marcus Webb) is proven absent from every rollup surface. **P-6 (July 25) mirrors this pattern in the opposite direction** — `assertConcernFrictionOnly()` in `src/lib/retraining-watch.ts` guarantees the reverse: no win/judgment record can ever reach the Coaching Watch surface.

⭐ **P-4.5 caught a real bug via the standing browser-eyeball rule, not the seed script.** The evidence-packet page double-URL-encoded a person's name in its client-side fetch ("marcus webb" → "marcus%2520webb", 404) — invisible to `scripts/seed-p4-5.mjs`'s DONE test because that harness never goes through the Next.js router/URL layer at all. Caught live clicking from the ranked list into Marcus Webb's card, fixed (decode defensively before re-encoding), redeployed, re-verified live. Filed as a standing lesson: a passing seed/API-level DONE test does not substitute for actually clicking through the deployed UI.

⭐⭐⭐ **THE PRESCRIPTION ENGINE IS COMPLETE AND LIVE (P-4A + P-4B, July 24).** Full loop shipped, deployed, seed-verified, and independently browser-verified end-to-end: detection → triage → pairing → ROI queue **(P-4A)** → manager gate → expert fidelity check → training generation (3 altitudes) → teach-back → efficacy loop + auto-escalation → regenerate **(P-4B)**.

⭐⭐⭐ **P-4B INDEPENDENT BROWSER VERIFICATION (July 24) — every build confirmed on live UI.** Walked `/prescriptions` and the clamping-drift detail page. Queue shows all four efficacy end-states simultaneously: **escalated** (rung 2→3, with the exact post-delivery recurrence record named) · **approved/in-flight** (HR capture-first) · **proven effective/closed** (conflict prescription, quiet 20 days vs. a 14-day window, labeled "Kirkpatrick Level 4, measured automatically") · **snoozed** (wakes 7/30, "snooze defers, never deletes"). Detail page confirmed: manager approval attributed to Elena Ruiz · expert fidelity check showing David Chen's ✓ *"Yes, that's how I think"* · three training altitudes (Floor / Supervisor / Executive) · **regenerate with v2/v1 versioning and the redesign reason preserved** · teach-back scored with full rubric feedback · efficacy loop state with escalation rationale.

**⭐ Three things the verification surfaced as genuinely strong:**
1. **Regenerate is a designer, not a re-roll.** The stored reason — *"Night crew won't sit through a walkthrough — give the facilitator something more hands-on"* — produced a materially different strategy (hands-on card-sorting diagnosis drill vs. a walkthrough). Constraint in, redesign out.
2. **Teach-back grades judgment, not recall.** Tom Whitfield's answer recited the framework correctly and still scored 40/100 — the grader caught that he treated already-given evidence as an open question, then recommended the play anyway, which is precisely the boundary violation the framework warns against. Grading *applied judgment in this situation* is the hard part of teach-back, and it worked.
3. **Blameless doctrine holds where it's most tempting to break.** The escalation names a *record* ("The Cold-Fixture Gate," dated after delivery) under evidence, with explicit copy: *"records, never people."* **P-4.5 extends this doctrine one level further: not just "name records, not people" in copy, but "never let a failure record reach a person-level view at all," enforced in code. P-6 extends it a third way: a manager-only surface can name a person, but only in aggregate pattern language, never framed as the person being the problem, and never visible to the person themselves.**

⭐ **P-4B same-day correction (already fixed + pushed).** After the initial clean run, `diag-efficacy.mjs` caught the escalation case going unsatisfiable on repeat runs: `delivered_at` recomputes to `now − 10 days` every run, but the planted recurrence record was gated on an *existence* check rather than a *freshness* check — so its fixed timestamp went stale and silently stopped post-dating delivery. Fixed in `scripts/seed-p4b.mjs` (commit `4698140`, dev-only seed script — deployed app `c65000f` unaffected). Re-run verified. **Generalized gotcha filed** (see Gotchas: relative-time idempotency guards need freshness checks, not existence checks).

⭐⭐⭐ **P-4A (July 23) — the demonstration that matters.** The clamping-drift prescription reconstructs a story across three experts over three months: David Chen solves fixture thermal drift on CNC Line 2 (4/26) → Tom Whitfield recognizes the same signature on Line 5 (7/15) → Elena Ruiz reads four weeks of scrap reports and names it a *training* gap, not an engineering one (7/20). The engine independently reached Elena's conclusion **and named who should teach it**. P-4B then proved the other half: when the fix genuinely doesn't transfer, the engine catches the recurrence and escalates instead of silently declaring victory. ⭐ Second win: on the HR gap, a 0.863-similarity framework ("The Onboarding Lottery Fix") was correctly ruled **adjacent-but-not-covering** — close ≠ covering fired exactly right.

✅ **ROI ranking bias — FIXED (P-5, July 25).** Was: ROI = recurrence × rung severity systematically ranked conflict-sourced prescriptions **last**, because conflicts are rung-clamped to ≤2. Now: queue sorts by **urgency first** (conflict=High · entity_signal=Medium · coverage_gap=Low), ROI as tiebreak — a live contradiction jumps the queue even at its capped rung. Both numbers shown (urgency chip + ROI chip). See DECISION-LOG 2026-07-25.

⭐⭐⭐ **P-3 FULLY CLOSED (July 23)** — contested badges render on both sides of the Priya×Angela conflict in `/library` AND `/retrieve`; surface-with-warning holds visually across the P-2 → P-3 → P-4A chain. **P-2 · P-1 · P-0.5 · P-0 — all DONE + DEPLOYED.** Entity Map field #8 feeds P-2, P-3, P-4A pairing, and now (P-4.5, P-6) mention aggregation in both directions.

⭐⭐⭐ **FOCUS LOCKED (July 21):** Track B ONLY — the enterprise "operating brain." Success gate = **$250K/year income replacement — hit it or pivot.**

⭐⭐⭐ **BUILD-FIRST LOCKED (July 22):** NO pitch/pilot/GTM until a polished, demo-able prototype. Warm pilot AFTER P-5 passes. **Prototype phase closed July 25 — this gate is now lifted; P-6 is the first feature built post-gate.**

**Data state:** real = 503 approved + 769 pending. Real = bdhicks83@gmail.com · test = bdhicks83+test1@gmail.com (⚠️ **NEVER DELETE — see gotcha: its sources parent 1,248 main-account insights via cascade**). **Demo org (reseed STAGED July 28 — the block below describes the AWIP target state; live still shows Meridian until Brian runs the sequence): "All Weather Insulated Panels (AWIP) — DEMO,"** Little Rock, AR plant as the primary site. **15 expert accounts** (`<first>.<last>@awip-demo.example` / `Demo-AWIP-2026!`): Joe Paparella (President) · Brian Montes (VP Ops) · Kim Harrell (VP Sales) · Brian Hicks (Sr. Dir HR & EHS) · Chuck Milner (Little Rock Plant Manager — the coaching-watch manager) · Brian Ng (Panel Technical Expert/R&D) · Dana Whitfield (Quality Manager — fictional) · Carlos Ramos (Maintenance) · Zach Davis (EHS) · Klaudia Donaghy (Talent) · John Dial (Inventory) · Ben Seger (Territory Mgr) · Marcus Webb (PM, the Win Column subject) · Richard Jenkins (Multi-Function Operator) · Tyler Brooks (Operator — fictional, the coaching subject, reports to Chuck). Old Meridian accounts survive in auth but are un-hooked from the org. **Library = the 10 IMP frameworks, VERBATIM** from `IMP-DEMO-FRAMEWORK-CONTENT.md`. **Demo spine conflict (OPEN):** Dana Whitfield "The No-Release Gate" ⚔ Brian Ng "The Controlled Restart Release" — same trigger (first run after a changeover), hold vs. release. **Second conflict (OPEN, cross-functional):** Kim Harrell "Custom Profile: Promise It or Walk" ⚔ Brian Montes "Capacity Reality First". **Prescriptions:** peel-check recurrence escalated rung 2 (job aid, didn't land) → rung 3 (hands-on drill under Richard Jenkins) + one proven-effective (coil-lot re-qualify checklist). **Win Column:** Marcus Webb — 3 leader write-ups (Kim Harrell, Brian Montes, Chuck Milner) for the cold-storage tolerance catch. **Knowledge gap (OPEN, Brian Ng fills it on camera):** between-setpoints panel-thickness policy. **Coaching Watch:** Tyler Brooks (2 records), visible only to Chuck Milner. **📌 Ad shoot retrieval query (IMP):** *"We had a delamination escape right after a profile changeover on the Little Rock line — should we release the next run before the bond-strength inspection clears?"* → must return "The Controlled Restart Release" (Brian Ng) top match + CONTESTED badge — `verify-awip-demo.mjs` asserts exactly this.

---

## 🧱 STACK (locked, unchanged)

Supabase (Postgres) · Next.js/TS · Inngest · Vercel · Claude (claude-sonnet-5) · Voyage AI (voyage-large-2 / 1536) · Remotion + ElevenLabs (Remotion now ACTIVE for the ad brand system — local render only, never Vercel) · no agent frameworks

Local: `C:\Users\BDHIC\Claude\Projects\LIT Repository\spiderweb` · Repo: `bdhicks83-ui/spiderweb` · Host: `spiderweb-nine.vercel.app` · Dev ritual: spiderweb-dev-environment.md

**Gotchas:** SQL editor `auth.uid()` doesn't resolve — use `a7d205f0-778c-44b9-9e13-4ebd5f47e964` · never assume `content[0]` is text — find first `type:"text"` block · delete `connections` before `insights` · always say local vs. live · **⚠️ AD-AUDIT (July 26) — NEVER delete the `bdhicks83+test1@gmail.com` account:** it looks like a pure test artifact (0 insights, 0 pattern_records of its own) but its 29 `sources` rows are the PARENT rows of **1,248 of the database's 1,272 insights** — owned by the MAIN account via `insights.source_id ON DELETE CASCADE`. Deleting it cascade-destroys the live insight web. It lives only in auth and appears on no on-camera surface; re-parent its sources first if it ever truly has to go · **P-3 threshold = 0.75 cosine (TUNED live)** — voyage-large-2 compresses cosine high (unrelated ~0.62–0.69, on-target ~0.85); P-4A reuses it verbatim — do not re-derive · **P-3:** `search_pattern_records_by_query` is SECURITY INVOKER on purpose (user RLS scopes it) · **P-4A:** `search_pattern_records_by_query_for_org` is SECURITY DEFINER **on purpose** (service-role detector, explicit org pin), EXECUTE revoked from anon/authenticated — do NOT collapse either into the other · **P-4A:** detections dedupe on `(org_id, dedupe_key)`; re-running detection is always safe · **P-4A:** rung ceilings (conflict ≤2, entity ≤3) clamp DOWN only; triage/coverage model failures = NO prescription (fail open) · **P-4A:** dept token-subset name merging applies to `department` entities ONLY — **people need their own conservative matcher (P-4.5 shipped this: exact-normalized-string only, deliberately not fuzzy — see DECISION-LOG) — prefer under-merging to over-merging; P-6 reuses this exact matcher (`normalizePersonKey`, imported directly from `win-column.ts`) rather than re-deriving it** · **P-4A:** `scripts/seed-p4a.mjs` copy-mirrors prescription.ts + pattern-embedding.ts + the two claude.ts helpers — keep in sync · **P-4A (fixed):** `let rung: number` needs explicit annotation (TS2322) · **P-3:** pattern_records embed as `document`, queries as `query` — don't mix · backfill/seed `buildEmbeddingText` is a verbatim mirror (copy-don't-import) · **P-0.5:** capture-time PII scrubbing intentionally OFF · **Supabase SQL editor runs a pasted script as ONE transaction — paste complete migrations as one block** · `_to_delete/` gitignored AND tsconfig-excluded (safe to empty) · **PowerShell 5.1: no `&&`** — separate lines or `if ($LASTEXITCODE -eq 0) { }` · raw multi-column `.select()` → cast to explicit row type · RLS on profiles querying profiles → `current_org_id()` SECURITY DEFINER, and **P-6 adds the same pattern for a narrower boundary: `is_manager_of(target_person_id)` SECURITY DEFINER checks `profiles.manager_id = auth.uid()` for one specific target row — use this shape whenever a policy needs "is the caller a specific relation of this row," not just "is the caller in this org"** · `tsconfig.p2/p3/p4a/p4b/p4-5.json` are ready scoped typecheck configs; local typecheck is the gate (no `tsconfig.p6.json` was created — gap, low priority) · client pages must NOT import server-only libs pulling `@/lib/claude` (fs) · **browser autofill hazard:** `/login` re-injects Brian's saved creds — set fields programmatically then submit, and **verify identity via `/settings` (shows the logged-in name) before trusting a persona-switch test** · **device bridge limits:** npm registry can be 403; NO outbound internet AND cannot git commit — live scripts AND git commands run from Brian's PowerShell, and **CORRECTED July 25 (P-7): do NOT run git AT ALL through the device bridge, not even read-only** — `git status` can leave an unremovable `.git/index.lock` and wedge the repo; audit uncommitted work by reading FILES, never git · **sign-out button shipped (P-5)** — `/dashboard` and `/settings` both have one; use it for persona switching instead of incognito · **browser-verify note:** clicking the queue's ROI/evidence text does not navigate — grab the row's `<a href>` directly when driving programmatically · **P-4B (confirmed):** Anthropic client needs explicit `timeout: 60_000` + `maxRetries: 0` on BOTH `src/lib/claude.ts` and seeds · **P-4B (confirmed):** any reasoning-heavy scoring/judging call needs `max_tokens: 8000`, not ~1000 — sonnet-5 can burn the whole budget on internal reasoning and emit an EMPTY text block at `stop_reason=max_tokens` · **P-4B (confirmed):** `parseJson`/`parseJsonLoose` run a `repairJsonControlChars` pass — keep mirrored between `src/lib/claude.ts` and seeds · **P-4B (confirmed):** `runEfficacyLoop` must exclude a prescription's own founding records from its recurrence match — keep mirrored between `src/lib/prescription.ts` and the seed · **P-4B:** seeded relative timestamps need **freshness checks**, not existence checks, on sibling planted records · **P-4B:** `scripts/diag-efficacy.mjs` is READ-ONLY — run it whenever efficacy escalation looks wrong, before touching code · **P-4.5:** a client page re-encoding a dynamic route param must decode defensively FIRST (`%20` → `%2520` silently 404s) — caught only by clicking the deployed UI · **P-4.5:** the wins-only guardrail is enforced ONCE, in `aggregateWinColumn()` — every future person-level surface MUST route through it · **P-5:** `/login` styling fixed; if `diag-library-count.mjs` is ever off by one, rerun the full chain once before assuming regression · **P-5:** `verify-p3.mjs` count is GLOBAL, `diag-library-count.mjs` is org-scoped — a mismatch is not a bug on its own · **P-5:** PowerShell `Tee-Object` writes UTF-16LE — prefer `| Out-File -Encoding utf8`, and `Test-Path` the file before trusting a redirect · **P-5:** the Claude-in-Chrome extension intermittently disconnects mid-session — re-verify field contents before submitting after any reconnect · **P-6:** `entity_map` people are free-text names — access-gated person surfaces can only cover registered accounts with the relational field the gate needs; check schema BEFORE speccing · **P-6:** re-detection upserts must decide explicitly whether new evidence reopens a dismissed row — `retraining_signals` only reopens on recurrence increasing · **P-7 — THE VERIFY-SCRIPT BLIND SPOT:** `verify-p3.mjs` uses the service-role key (bypasses RLS) while `/api/retrieve` runs under user RLS — a passing service-role script proves nothing about a bug living in that gap; `scripts/diag-retrieve.mjs` (READ-ONLY) runs the route's exact two-step read at BOTH privilege levels — run it BEFORE touching retrieval code · **P-7:** never compute an "is it empty" flag from one read and the payload from a different read — derive the flag from the payload that ships · **AD/REMOTION (July 27):** `src/remotion/brand/tokens.ts` is a VERBATIM MIRROR of `src/styles/theme.css` (copy-don't-import — Remotion's Chromium never loads the app stylesheet); if theme.css changes, change tokens.ts too · **REBRAND (July 27): the hex-mirror set is now THREE files** — `remotion/brand/tokens.ts` + the `COLORS` blocks in `src/lib/framework-pdf.tsx` and `src/lib/resume-pdf.tsx` (react-pdf's layout engine never sees the DOM, so CSS vars are unavailable there); a theme.css change fans out to all three · **REBRAND (July 27):** `LOGO_SRC` now points at `"brand/viridescent-stacked-reversed.png"` (verified on disk) — Remotion's `<Img>` still HARD-FAILS the whole render on a missing `staticFile()` path (no fallback), so if that PNG is ever moved/renamed, flip `LOGO_SRC` back to `null` FIRST (revert instruction is in the file) · Remotion renders LOCAL ONLY (no Chromium on Vercel serverless) — never wire it to a Vercel route · all ad copy/timing lives in `src/remotion/ad/ad-script.ts` — components hardcode no content; edit only that file.

---

## ✅ WHAT'S BUILT

| Built | Status | Role in Track B |
|---|---|---|
| Viridescent rebrand (theme.css token system + 3 hex mirrors · every page token-converted · naming sweep · 8 logo PNGs wired · `LOGO_SRC` live · camera-readiness code pass) | ✅ **VERIFIED COMPLETE LOCAL Jul 27 · UNCOMMITTED — live still OLD branding** | ⭐⭐ Customer-facing brand for the ad shoot + pilot outreach |
| Ad-prep: live demo-data audit + Remotion brand system (`brand/` components · `Ad90` composition · `npm run render:ad`) | ✅ **Audit DONE on LIVE Jul 26–27 · first 1080p render PASSED LOCAL Jul 27 · UNCOMMITTED** | ⭐⭐ Unblocks the 90-sec ad shoot; brand system reused for every derivative cut |
| P-6 Coaching Watch (manager_id + `is_manager_of()` RLS · `retraining_signals` · concern/friction detection · concern/friction-only guardrail · manager-private page + dashboard tile) | ✅ **DONE + DEPLOYED + seed-verified + browser-verified Jul 25** | ⭐⭐⭐ First post-prototype feature — early coaching signal, structurally incapable of leaking to peers or the named person |
| P-4.5 Win Column (mention aggregation · ranked view · corroboration badges · context chips · cross-dept badge · rising signal · retention watch · evidence packet · wins-only rollup enforced in code) | ✅ **DONE + DEPLOYED + seed-verified + browser-verified Jul 24** | ⭐⭐⭐ Unsolicited third-party attribution — a recognition signal no self-nomination can match |
| P-4B Prescription Engine pt 2 (manager gate · fidelity check · training gen 3 altitudes · teach-back · efficacy loop + auto-escalation · regenerate · seed/verify harness) | ✅ **DONE + DEPLOYED + seed-verified + independently browser-verified Jul 24** | ⭐⭐⭐ Closes the full Prescription Engine loop |
| P-4A Prescription Engine pt 1 (detection rows · triage ladder w/ stored rationale · auto-pairing w/ capture-first · ROI queue + evidence-chain detail) | ✅ **DONE + DEPLOYED + browser-verified Jul 23** | ⭐⭐⭐ The payoff feature's front half |
| P-3 Contextual retrieval | ✅ **FULLY CLOSED Jul 23** | ⭐⭐⭐ The Copilot moment; semantic substrate for gap-finding |
| P-2 Conflict X-ray | ✅ **DONE + DEPLOYED Jul 23** — badges confirmed in `/library` AND `/retrieve` | ⭐⭐⭐ Detection input #1 for P-4A |
| P-1 Org/multi-user foundation | ✅ **DONE + DEPLOYED Jul 23** — org isolation browser-verified | ⭐⭐⭐ Foundation for everything org-scoped |
| P0 Elicitation Engine (/codify) | ✅ Done Jul 21 · hardened Jul 22 | ⭐⭐⭐ Capture |
| P-0.5 Methodology Router + Entity Map + guardrails | ✅ **DONE + DEPLOYED Jul 23** | ⭐⭐⭐ Entity map feeds P-2, P-3, P-4A, P-4.5, P-6 |
| Ask Your Spiderweb / Phase 6 | ✅ Built | Grounded-answer pattern reused by P-3 |
| Belief-revision depth gate | ✅ Locked · reused in P-2 | Conflict resolution + versioning |
| Phase 7 flag-never-block | ✅ Shipped · extended by P-2/P-3/P-4B snooze AND P-4.5 wins-only rollup AND P-6 concern/friction-only rollup | Doctrine |
| Multi-format output | ✅ Built | → P-4B training altitudes |
| Upload · Dashboard · Approve | ✅ Live | Supporting surfaces |

**❌ Cut permanently:** benchmarking · identity verification · external proof scoring · consumer marketplace · ERP/KPI/meeting ingestion

---

## 🛣️ PROTOTYPE PHASES (plan of record: ROADMAP v4)

| Phase | What | Status / DONE test |
|---|---|---|
| **P-0 Harden** | Flake fix · migration confirm | ✅ **DONE + DEPLOYED Jul 22** |
| **P-0.5 Capture upgrades** | Methodology router · Entity Map · caps · TTFV | ✅ **DONE + DEPLOYED Jul 23** |
| **P-1 Org / multi-user** | Org table · RLS · shared library · demo seed · persona picker | ✅ **DONE + DEPLOYED Jul 23** |
| **P-2 Conflict X-ray** | Cross-user detection · surface-with-warning badge · review UI | ✅ **DONE + DEPLOYED Jul 23** |
| **P-3 Contextual retrieval** | Situation → framework · embeddings · silent-fail fix · contested badges | ✅ **FULLY CLOSED Jul 23** |
| **P-4A Prescription Engine pt 1** ⭐ | Detection → triage ladder → auto-pairing → ROI queue + evidence chain | ✅ **DONE + DEPLOYED + browser-verified Jul 23** |
| **P-4B Prescription Engine pt 2** ⭐ | Manager gate → fidelity check → training (3 altitudes) → teach-back → efficacy loop + escalation → regenerate | ✅ **FULLY CLOSED Jul 24** — deployed `c65000f`, freshness fix `4698140`, independently browser-walked |
| **P-4.5 Win Column** | Mention aggregation · corroboration badges · context chips · evidence packet · rising signal · retention watch · **wins-only rollup enforced in code** | ✅ **FULLY CLOSED Jul 24** — browser-verified live; double-URL-encoding bug caught by the eyeball, fixed same-session |
| **P-5 Polish + demo** | Punch list, all 11 items | ✅ **FULLY CLOSED Jul 25** — full loop walked live in 5:02. **Prototype complete — build-first gate lifted.** |
| **P-6 Coaching Watch** (first post-prototype feature) | `manager_id` + `is_manager_of()` RLS · `retraining_signals` · concern/friction detection (2+ threshold) · concern/friction-only guardrail · manager-private `/coaching` page + tile | ✅ **DONE + DEPLOYED + seed-verified + browser-verified Jul 25** — RLS boundary confirmed three ways at the network layer |
| **Ad-prep** (Jul 26–27) | Job 1: live demo-data audit · Job 2: Remotion brand system + Ad90 render | ✅ **DONE** — audit fixed/verified on LIVE; `npm run render:ad` passed LOCAL (1920×1080/30fps). **Uncommitted — commit from Brian's PowerShell** |
| **Viridescent rebrand** (Jul 27, session 3) | Palette token system + mirrors · user-visible naming sweep · 8 logo PNGs wired · `LOGO_SRC` flipped · Job-4 camera-readiness code pass | ✅ **CLOSED LOCAL Jul 27** — `tsc --noEmit` clean · browser-verified on local dev · **LIVE pending commit + deploy** · authenticated visual walk pending one login from Brian |

**🅿️ v2 (post-pilot):** Day-one onboarding path · voyage-4 migration.

---

## ✅ OPEN LOOPS

### 🔴 Blocking / near-term

**Ad shoot — Brian's steps before recording:**
0. **Run the AWIP reseed sequence** (wipe SQL in the Supabase SQL editor, then the 6 node commands — exact order in the AWIP block at the top of this doc). The die-changeover storyline is retired; the shoot runs on the IMP content.
1. **Commit the whole tree from your PowerShell** (ad-prep + the full Viridescent rebrand — ~45 modified files plus new `public/brand/` PNGs, `manifest.webmanifest`, `LogoStills.tsx`; the stray `diag-*.txt` / `*.log` / `verify-*.txt` outputs are optional). Push → Vercel deploys → **live flips to Viridescent.**
2. ~~Drop the logo PNG + flip `LOGO_SRC`~~ — **DONE July 27:** all 8 Viridescent PNGs exist in `public/brand/` and `LOGO_SRC` already points at `brand/viridescent-stacked-reversed.png`.
3. **Paste the real script lines from `AD-SCRIPT-90sec-2026-07-26.md` over the placeholders in `src/remotion/ad/ad-script.ts`** (the only file to edit — beats, title lines, caption cues, all timings in one place). Then `npm run remotion:studio` to preview, `npm run render:ad` to output.
4. **Log in once (local or live) so Claude can drive the authenticated Job-4 visual walk** — `/library` · conflict review · `/retrieve` · training module · Win Column. Code-level pass is already clean; the eyeball walk is the standing rule.

⚠️ One UNRESOLVED report (unchanged): the `/api/retrieve` break Brian saw on **local** was never reproduced — live re-verified healthy July 27 (6/6 ad-audit queries). If it recurs, capture status + body, run `node scripts/diag-retrieve.mjs`.

### 🟠 Active now (gate lifted July 25)

90-sec ad shoot (screen capture + VO over the verified storyline; retrieval query string is in Data state above) · warm pilot outreach · demo script voice pass · IP-clarity counsel · second warm lead · enterprise pricing

### 🟡 Decisions not locked

Brand-voice sign-off · 769 pending insights · P-3 threshold 0.75 (revisit only on real misses) · whether the demo script gets a 9th beat for Coaching Watch, and whether Coaching Watch is customer-facing in the pitch at all · real ad copy vs. the placeholder lines in `ad-script.ts` (placeholders written to the same storyline/cadence, but the words are not Brian-approved copy)

### 🟢 Doc + copy debt

Empty `_to_delete/` · MANIFESTO.md · ARCHITECTURE.md (stale — predates P-0.5 through P-6) · DECISION-LOG sprawl · no `tsconfig.p6.json` · repo has stray diagnostic outputs (`diag-*.txt`, `*.log`, `verify-*.txt`) — untracked, harmless, delete or gitignore at leisure · Humanbloom code comments + `hb-foundation.css` filename (cosmetic, deliberately parked) · dev-only hydration-mismatch console warning on `/` (body inline style camelCase vs. longhand — no visual effect, not camera-visible)

---

## 🅿️ PARKING LOT (recoverable)

Solo wedge · creators · teachers · L&D-standalone · Archive jogger · Asset Strength solo · consumer marketplace · Bloom Consulting · video pipeline (partially revived: Remotion brand system now ACTIVE for the ad) · AI-twin · Day-one onboarding path (v2) · voyage-4 migration · all prior framings · **the code rename (Humanbloom→Viridescent in identifiers/repo/host/DB) — "language now, code later" still holds.**

---

## 👤 BRIAN'S ROLE

Solo Founder & CEO — final approver. Delegates execution; autonomous within session. Report back before finalizing: brand voice · pricing · anything customer-facing.

## 🧠 HOW BRIAN WORKS BEST (standing, ADHD, non-negotiable)

Short chunks · bullets/tables · ONE question at a time · phases w/ headers, current step only · batched setup · full files never snippets · heredoc for large pastes · **PowerShell-safe paste blocks (no `&&`)** · **SQL pasted directly in chat, never "go get the file"** · proactive Decision Log entries · flag heavier-tool handoffs · secrets out of chat.

**⭐ STANDING (July 23): ALWAYS run the browser eyeball myself.** Every build's DONE test includes driving Claude-in-Chrome to load the deployed UI, walk the feature's pages + one detail view, and visually confirm — never offer it as optional or hand it to Brian. Note: I cannot type passwords into fields — if no session is live, Brian logs in once and I drive everything after. **Reinforced by P-4.5 (July 24): the eyeball caught a real 404 bug a passing seed-script DONE test never would have surfaced.** **Reinforced again by P-6 (July 25): for any feature where a person-level negative signal exists at all, the access-boundary browser-verify must be driven as three separate logged-in identities (WITH access, peer WITHOUT, and the subject), each identity confirmed via `/settings` before testing.**

**⭐ STANDING (July 24): git commits/pushes run from Brian's PowerShell, never the device bridge.** **CORRECTED July 25 (P-7): run NO git through the device bridge, read-only included** — `git status` left an unremovable `.git/index.lock` and wedged the repo. Audit uncommitted work by reading the files themselves; hand Brian a paste block for anything git-shaped. (Note: the July 27 ad-prep session ran as Claude Code directly on Brian's machine, where git works normally — the bridge rule applies to device-bridge/Cowork sessions.)
