// Track B demo scope — hidden for Track B demo, recoverable.
// Single toggle: flip to false to restore every Track A / legacy surface
// (resume builder, credibility score, portfolio value, profile verification,
// pending-insights counter, upload/approve links, logged-in homepage).
// No code, routes, components, or tables were deleted — UI visibility only.
// Safe to import from client AND server components (constant only, no deps).
export const HIDE_TRACK_A = true;
